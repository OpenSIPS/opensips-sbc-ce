services:
  mysql:
    build:
      context: ./etc/mysql/image
    container_name: mysql
    command:
      - --bind-address=127.0.0.1,${HOST_IP}
    volumes:
      - ./etc/mysql:/docker-entrypoint-initdb.d
      - db:/var/lib/mysql
    env_file:
      - .env
    environment:
      - MYSQL_IP=localhost
    healthcheck:
      test: mysql -h ${MYSQL_IP} -P ${MYSQL_PORT} -u ${MYSQL_USER} -p'${MYSQL_PASSWORD}' opensips -e 'SHOW TABLES LIKE "location"' 2>&1 | grep -q location
      interval: 10s
      timeout: 1s
      retries: 5
      start_period: 20s
    network_mode: host

  opensips:
    image: ghcr.io/opensips/opensips-sbc-ce-config/opensips:sbc-ce
    container_name: opensips
    volumes:
      - ./etc/opensips:/etc/opensips
    env_file:
      - .env
    depends_on:
      mysql:
        condition: service_healthy
    command:
      - -p /etc/opensips/substenv.sh
    network_mode: host

  opensips-cp:
    build:
      context: ./etc/opensips-cp/image
    container_name: opensips-cp
    env_file:
      - .env
    depends_on:
      mysql:
        condition: service_healthy
    volumes:
      - ./etc/opensips-cp/docker-entrypoint.d:/docker-entrypoint.d
      - type: bind
        source: ./etc/mysql/functions
        target: /docker-entrypoint.d/functions
    network_mode: host

  rtpengine:
      image: fonoster/rtpengine:latest
      container_name: rtpengine
      environment:
        RTPENGINE_PUBLIC_IP: ${RTPENGINE_PUBLIC_IP}
        RTPENGINE_PORT_MIN: ${MINPORT}
        RTPENGINE_PORT_MAX: ${MAXPORT}
        RTPENGINE_BIND_NG_PORT: ${RTPENGINE_PORT}
        RTPENGINE_BIND_NG_IP: ${RTPENGINE_IP}
        RTPENGINE_LOG_LEVEL: ${RTPENGINE_LOG_LEVEL}
      network_mode: host

volumes:
  db:
